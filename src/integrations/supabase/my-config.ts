// Public connection details for this project's own Supabase instance.
// The publishable key is safe to expose in the browser.
export const MY_SUPABASE_URL = import.meta.env["VITE_MY_SUPABASE_URL"] as string;
export const MY_SUPABASE_PUBLISHABLE_KEY = import.meta.env["VITE_MY_SUPABASE_PUBLISHABLE_KEY"] as string;
